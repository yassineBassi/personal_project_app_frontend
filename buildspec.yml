---
version: 0.2
env: 
  variables:
    BACKEND_DNS:
phases: 
  install:
    runtime-versions:
      nodejs: 18
    commands: 
      - echo "insalling tools"
      - npm --v
      - node -v
  pre_build:
    commands: 
      - echo "installing dependencies"
      - npm install
      - sed -i "s|BACKEND_DOMAIN|$BACKEND_DNS|g" src/environments/environment.prod.ts
  build:
    commands:
      - npm run build

artifacts: 
  files: 
    - "**/*"
  base-directory: dist/frontend/browser